# Формальная часть HCA-DSE (Section 4 рукописи)

Все утверждения привязаны к конкретному коду и к конкретному тесту — см. таблицу в
конце. Обозначения подобраны так, чтобы переносились в LaTeX без переделки.

---

## 4.1 Пространство проектирования и частичные архитектуры

Пространство решений — декартово произведение конечных доменов, упорядоченных
фиксированной перестановкой переменных \(v_1,\dots,v_k\):

$$\mathcal X = D_1\times\dots\times D_k,\qquad |\mathcal X|=\prod_{i=1}^{k}|D_i|.$$

**Определение 1 (частичная архитектура).** Частичная архитектура глубины \(d\) — это
префикс присвоений \(p=(a_1,\dots,a_d)\), \(a_i\in D_i\). Множество её завершений

$$\mathcal C(p)=\{x\in\mathcal X: x_i=a_i,\ i=1,\dots,d\},\qquad
|\mathcal C(p)|=\prod_{i=d+1}^{k}|D_i|.$$

\(\mathcal C(p)\) для \(d=0\) есть всё \(\mathcal X\); для \(d=k\) — одноэлементное
множество. Множество допустимых архитектур

$$\mathcal F=\{x\in\mathcal X: g_j(x)\le g_j^{\max},\ j=1,\dots,m\},$$

вектор целей \(F(x)=(f_1(x),\dots,f_n(x))\), все минимизируются, \(n=4\):
задержка \(L\), энергия \(E\), стоимость \(C\), объём трафика \(D\).
Парето-фронт в пространстве целей:

$$\mathcal P^\star=\{F(x): x\in\mathcal F_\sigma,\ \nexists\,y\in\mathcal F_\sigma:\ F(y)\preceq F(x)\},$$

где \(\mathcal F_\sigma\) — допустимые и структурно корректные архитектуры
(предикат \(\sigma\) определён в Предложении 3),

где \(u\preceq v\) означает \(u_i\le v_i\ \forall i\) и \(u_j<v_j\) хотя бы для одного \(j\).

---

## 4.2 Допустимые оценки (admissible bounds)

**Определение 2 (допустимость).** Функция \(LB_i\) допустима для цели \(f_i\), если

$$LB_i(p)\le f_i(x)\qquad \forall p,\ \forall x\in\mathcal C(p).$$

Ключевая техническая трудность в heterogeneous edge–cloud CPS: цели не являются
*assignment monotonous* — не каждое присвоение только добавляет стоимость. Задержка
содержит член ожидания в очереди, который зависит от загрузки тира \(\rho_t\), а та
определяется совместно числом узлов, классом узла, коэффициентом репликации и
политикой размещения. Поэтому стандартный приём «стоимость частичного решения плюс
оптимистичный остаток» неприменим напрямую, и нужны две отдельные конструкции.

### Лемма 1 (потерминная релаксация)

Пусть цель разлагается в сумму неотрицательных членов

$$f_i(x)=\sum_{t\in T_i}\varphi_t(x),$$

и для каждого члена определено

$$\underline{\varphi}_t(p)=\min_{x\in\mathcal C(p)}\varphi_t(x).$$

Тогда \(LB_i(p)=\sum_{t\in T_i}\underline{\varphi}_t(p)\) допустима.

*Доказательство.* Для любого \(x\in\mathcal C(p)\) и любого \(t\) выполнено
\(\underline{\varphi}_t(p)\le\varphi_t(x)\) по определению минимума. Суммируя по
\(t\in T_i\), получаем \(LB_i(p)\le f_i(x)\). \(\square\)

Существенно, что минимумы берутся **независимо по членам**: комбинация значений
переменных, реализующая эти минимумы, может не соответствовать ни одному реальному
завершению. Это ослабляет оценку, но не нарушает допустимость, и позволяет вычислять
\(LB_i(p)\) за \(O(1)\) по предвычисленным экстремумам доменов.

Каждый член \(\varphi_t\) в нашей модели монотонен по каждой свободной переменной
(время обработки убывает по \(C\), время передачи убывает по \(B\), стоимость и
статическая энергия возрастают по числу узлов, трафик возрастает по репликации),
поэтому \(\underline{\varphi}_t(p)\) достигается на границе домена и берётся из
таблицы, а не поиском.

### Лемма 2 (точность на определённых членах)

Пусть \(\mathrm{supp}(\varphi_t)\) — множество переменных, от которых зависит член
\(\varphi_t\). Если \(\mathrm{supp}(\varphi_t)\subseteq\{v_1,\dots,v_d\}\), то
\(\varphi_t\) постоянен на \(\mathcal C(p)\), и подстановка его точного значения
вместо \(\underline{\varphi}_t(p)\) сохраняет допустимость и не ослабляет оценку.

*Доказательство.* Постоянство очевидно; точное значение равно минимуму по
\(\mathcal C(p)\), поэтому Лемма 1 применима без изменений. \(\square\)

**Следствие (почему порядок переменных важен).** Порядок \(v_1,\dots,v_k\) выбран так,
что детерминанты загрузки тира (число узлов, класс узла, репликация, политика)
присваиваются раньше остальных. Как только они зафиксированы, член ожидания

$$W_t=\frac{w}{C_t}\cdot\frac{\rho_t}{2(1-\rho_t)}$$

становится определённым по Лемме 2 и входит в оценку точно, а не нулём. Именно это
превращает contention-зависимую цель, для которой assignment monotonicity не
выполняется, в цель с точно вычислимой частичной оценкой. До фиксации детерминантов
используется тривиальная допустимая оценка \(W_t\ge 0\).

Если при зафиксированных детерминантах получается \(\rho_t\ge 1\), то ни одно
завершение не имеет конечной задержки, и полагается \(LB_L(p)=\infty\), что также
допустимо.

---

## 4.3 Два правила безопасного отсечения

### Предложение 1 (отсечение по недопустимости)

Пусть \(LB_{g_j}\) допустима для ограничения \(g_j\). Если
\(LB_{g_j}(p)>g_j^{\max}\), то \(\mathcal C(p)\cap\mathcal F=\varnothing\).

*Доказательство.* Для любого \(x\in\mathcal C(p)\) имеем
\(g_j(x)\ge LB_{g_j}(p)>g_j^{\max}\), то есть \(x\notin\mathcal F\). \(\square\)

### Предложение 2 (отсечение по доминированию)

Пусть \(y\in\mathcal F\) — полностью оценённая допустимая архитектура, и

$$F(y)\preceq LB(p)=\big(LB_1(p),\dots,LB_n(p)\big).$$

Тогда \(F(x)\notin\mathcal P^\star\) для всех \(x\in\mathcal C(p)\).

*Доказательство.* Пусть \(x\in\mathcal C(p)\). По допустимости
\(f_i(x)\ge LB_i(p)\ge F_i(y)\) для всех \(i\). По условию существует \(j\) с
\(F_j(y)<LB_j(p)\le f_j(x)\). Значит \(F(y)\preceq F(x)\) со строгим неравенством по
координате \(j\), то есть \(y\) строго доминирует \(x\), и \(F(x)\) не принадлежит
Парето-фронту. \(\square\)

### Предложение 3 (замкнутость структурных инвариантов)

Пусть \(\sigma:\mathcal X\to\{0,1\}\) — предикат корректности архитектуры
(число шлюзов не превышает числа edge-узлов; фактор репликации не превышает числа
edge-узлов; репликация stage 1 не определена при политике, размещающей stage 1 вне
edge-тира). Рабочее пространство определяется как
\(\mathcal X_\sigma=\{x\in\mathcal X:\sigma(x)=1\}\), и все дальнейшие
утверждения формулируются относительно \(\mathcal F_\sigma=\mathcal F\cap\mathcal X_\sigma\).

Каждый инвариант зависит только от присвоенных переменных, и порядок
\(v_1,\dots,v_k\) выбран так, что все переменные каждого инварианта присваиваются
до момента его проверки. Присвоения не меняются, поэтому нарушенный инвариант остаётся
нарушенным:

$$\neg\sigma(p)\ \Rightarrow\ \sigma(x)=0\quad\forall x\in\mathcal C(p),$$

то есть \(\mathcal C(p)\cap\mathcal X_\sigma=\varnothing\). \(\square\)

**Важно (и это надо честно написать в статье).** Структурные инварианты — это
определение корректной архитектуры, а не следствие ограничений задачи: evaluator
формально способен вычислить цели и для архитектуры с репликацией при
cloud-first-политике, хотя такая конфигурация лишена смысла в модели (реплицировать
нечего). Поэтому структурное отсечение не оправдывается недопустимостью, а задаёт
пространство поиска. Исчерпывающий перебор, с которым проводится сравнение,
использует **тот же** предикат \(\sigma\), поэтому сравнение корректно. Этот факт
был обнаружен тестом `tests/test_pruning_safety.py`, который изначально проверял
структурное отсечение как условие недопустимости и нашёл контрпример.

---

## 4.4 Основная теорема

### Теорема 1 (сохранение Парето-фронта)

Пусть все используемые оценки допустимы (Леммы 1–2), отсечение выполняется только по
Предложениям 1–3, а архив содержит только допустимые полностью оценённые архитектуры.
Тогда множество недоминируемых векторов, возвращаемое HCA-DSE, совпадает с истинным
Парето-фронтом:

$$\mathcal P^{\mathrm{HCA}}=\mathcal P^\star.$$

*Доказательство.* Включение \(\mathcal P^{\mathrm{HCA}}\subseteq\mathcal P^\star\):
алгоритм оценивает архитектуры тем же evaluator'ом, что и исчерпывающий перебор, и
выдаёт недоминируемое подмножество оценённых допустимых архитектур; любой возвращённый
вектор реализуется реальной архитектурой, а недоминируемость относительно всего
\(\mathcal F\) следует из того, что доминирующая архитектура сама была бы либо
оценена, либо отсечена лишь по Предложению 2, что по нему же исключено.

Включение \(\mathcal P^\star\subseteq\mathcal P^{\mathrm{HCA}}\): пусть
\(u\in\mathcal P^\star\) и \(x\in\mathcal F\) — архитектура с \(F(x)=u\). Обход
представляет собой дерево префиксов, и \(x\) достигается по единственному пути
\(p_0\subset p_1\subset\dots\subset p_k=x\). Если бы какой-то \(p_d\) был отсечён, то
по Предложению 3 или 1 имели бы \(x\notin\mathcal F\) — противоречие, либо по
Предложению 2 существовала бы допустимая \(y\), строго доминирующая \(x\), что
противоречит \(u\in\mathcal P^\star\). Следовательно ни один \(p_d\) не отсечён, \(x\)
полностью оценивается, попадает в множество кандидатов, и \(u\) остаётся после
недоминируемой фильтрации. \(\square\)

**Замечание 1 (дубликаты).** Теорема сформулирована в пространстве целей. Если две
различные архитектуры имеют одинаковый вектор целей, отсечение по Предложению 2 может
удалить одну из них: условие \(F(y)\preceq LB(p)\) со строгим неравенством по одной
координате не выполняется при полном совпадении, но может выполняться для другого
доминирующего \(y\). Фронт при этом не теряется; теряется лишь дублирующее решение.
Это соответствует тому, что проверяется в тестах (сравнение множеств векторов целей).

**Замечание 2 (независимость от порядка обхода).** Предложение 2 верно для любого
архива допустимых оценённых архитектур, поэтому порядок обхода дерева влияет только на
эффективность отсечения, но не на корректность результата.

---

## 4.5 Сложность

Обозначим \(C_E\) — стоимость полной оценки design point, \(C_P\) — стоимость
вычисления оценок в одном узле дерева, \(N\) — число посещённых частичных состояний,
\(N_E\) — число полных оценок, \(|A|\) — размер архива недоминируемых точек.

$$T_{\text{exh}}=O\big(|\mathcal X|\cdot C_E\big),\qquad
T_{\text{HCA}}=O\big(N\,(C_P+n|A|)+N_E\,C_E\big).$$

Отсечение выгодно, когда

$$C_E\;>\;\frac{N\,(C_P+n|A|)}{|\mathcal X|-N_E}.$$

Измеренная точка безубыточности на S2/S3 — \(C_E\approx 1\text{–}3\ \mu s\), то есть
практически любой нетривиальный evaluator её превышает; при
дискретно-событийном evaluator'е \(C_E=19.4\) мс. Существенно, что \(N\ll|\mathcal X|\):
на пространстве \(8.3\cdot 10^{7}\) кандидатов посещается порядка \(1.4\cdot10^{4}\)
частичных состояний, то есть декартово произведение никогда не материализуется, а
пиковая память остаётся на уровне десятков килобайт.

---

## 4.6 Что доказано, что проверено, где это в коде

| Утверждение | Реализация | Проверка |
|---|---|---|
| Лемма 1 (потерминная релаксация) | `hcadse/bounds.py: lower_bounds`, кэш `hcadse/bounder.py: Bounder.bounds` | `tests/test_core.py: test_bound_admissibility` — исчерпывающе: все префиксы x все завершения на S1 x 3 workload |
| Лемма 2 (точный член очереди) | `hcadse/bounds.py: _queue_bound` | там же; плюс `tests/test_regression.py: test_cached_bounds_match_reference` |
| Предложение 1 | `hcadse/bounds.py: infeasible_by_bounds` | `tests/test_regression.py: test_exactness_all_variants` (вариант bounds-only) |
| Предложение 2 | `hcadse/bounds.py: dominated_by_bounds` | `tests/test_pruning_safety.py` (прямая проверка: ни одно завершение отсечённого поддерева не лежит на исчерпывающем фронте) |
| Предложение 3 | `hcadse/search.py: structurally_valid` | `tests/test_pruning_safety.py` (ни одно завершение отсечённого префикса не является структурно корректным) |
| Теорема 1 | `hcadse/search.py: hca_dse` | `tests/test_regression.py: test_exactness_all_variants` — S1, S2, S3 x 3 workload x 3 конфигурации отсечения |
| Раздел 4.5 (сложность, безубыточность) | — | `experiments/e2_costsweep.py`, `results/e2_costsweep.csv` |

Формулировка для статьи: **теоретическая корректность** (Теорема 1) и
**корректность реализации** (совпадение с исчерпывающим перебором) проверяются
раздельно и обе заявляются как результат.
